@model ProductsSearchViewModel

@if(@Model.Products.Count() > 0) {
    @foreach (var p in Model.Products) {
        @Html.Partial("ProductSummary", p)
    }
} else {
    <h1>Упс... Кажется этого у нас нет :(</h1>
}

